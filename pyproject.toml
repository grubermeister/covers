[project]
name = "worldcovers"
dynamic = ["version"]
requires-python = "==3.13.*"
dependencies = [
    "asgiref==3.11.1",
    "astroid==3.3.11",
    "attrs==25.4.0",
    "build==1.5.0",
    "click==8.4.2",
    "diff-match-patch==20241021",
    "dill==0.4.1",
    "django==5.2.7",
    "django-admin-logs==1.5.0",
    "django-allauth==65.18.0",
    "django-colorfield==0.14.0",
    "django-cors-headers==4.9.0",
    "django-debug-toolbar==6.0.0",
    "django-extensions==4.1",
    "django-filter==25.2",
    "django-import-export==4.4.1",
    "django-reversion",
    "django-reversion-compare",
    "django-types==0.24.0",
    "djangorestframework==3.17.1",
    "dotenv-cli==3.4.1",
    "drf-spectacular==0.29.0",
    "gunicorn==21.2.0",
    "inflection==0.5.1",
    "isort==6.1.0",
    "jsonschema==4.26.0",
    "jsonschema-specifications==2025.9.1",
    "mccabe==0.7.0",
    "mysqlclient==2.2.8",
    "packaging==25.0",
    "pandas",
    "pillow==12.3.0",
    "platformdirs==4.10.0",
    "pylint==3.3.9",
    "pylint-django==2.7.0",
    "pylint-plugin-utils==0.9.0",
    "pyproject-hooks==1.2.0",
    "python-decouple==3.8",
    "python-dotenv==1.2.2",
    "pyyaml==6.0.3",
    "referencing==0.37.0",
    "rpds-py==0.28.0",
    "sqlparse==0.5.5",
    "tablib==3.9.0",
    "tomlkit==0.13.3",
    "types-psycopg2==2.9.21.20251012",
    "uritemplate==4.2.0",
]

[project.scripts]
# Installed into .venv/bin/ when uv builds the project. Lets you run
# 'woco runserver' inside the venv (after 'source .venv/bin/activate',
# or via 'uv run woco runserver' without activation). The bash shim at
# ./woco wraps this so it works from a cold shell too.
woco = "woco_cli:main"

[build-system]
# Required so the project is installable, which is required for the
# console script above to land in .venv/bin/.
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
# Single source of truth for the app version, shared with the frontend
# (frontend/vite.config.ts reads this same file to build __APP_VERSION__).
# Bump the VERSION file at repo root to release a new version.
path = "VERSION"
pattern = "^(?P<version>.+)$"

[tool.hatch.build.targets.wheel]
# Only ship the CLI shim. backend/ stays on PYTHONPATH at runtime via
# woco_cli.py and is not packaged.
include = ["woco_cli.py"]

[dependency-groups]
# PEP 735 dev group. Installed locally by default with 'uv sync';
# excluded on the server with 'uv sync --no-dev'.
dev = [
    "anthropic",
    "ipykernel",
    "jinja2",
    "numpy",
    "openai",
]
